# 10 — 계약: 아키텍처와 데이터 계약 (구축 실행 세트 v0.6-exec)
> §3 · §4 원문. Phase 파일의 §표기는 전부 이 문서를 가리킨다.
> 주의: 배포물(SKILL.md 등)에는 이 문서를 참조로 남기지 말고 필요한 계약을 본문에 전사한다 (00-orchestrator.md §2).

---

## 3. 아키텍처

### 3.1 플러그인 저장소 구조 (배포물)

```
ticket-pilot/
├── .claude-plugin/
│   ├── plugin.json            # 이름·버전·설명 (이 폴더에는 매니페스트만 둔다)
│   └── marketplace.json       # 로컬/깃허브 마켓플레이스 등록용
├── commands/                  # 얇은 래퍼 4개 (각 1~3줄)
│   ├── init.md                #  → project-setup 스킬 실행
│   ├── tickets.md             #  → ticket-create 스킬 실행
│   ├── run.md                 #  → ticket-run 스킬 실행
│   └── done.md                #  → day-close 스킬 실행
├── skills/
│   ├── project-setup/SKILL.md
│   ├── ticket-create/
│   │   ├── SKILL.md
│   │   └── assets/board.html  # 보드 템플릿 (§4.2)
│   ├── ticket-run/SKILL.md    # [v0.5.1] 마무리(후속 제안·blocked 요약) 흡수 — 구 ticket-optimize
│   └── day-close/SKILL.md     # [v0.5.1] 메모리 3계층 압축 흡수 — 구 memory-optimize
├── agents/
│   └── tp-worker.md           # [v0.5.0] 병렬 실행 워커 정의 (§4.8)
├── hooks/                     # [보류] Step 4.4 참고
└── README.md
```

### 3.2 설치된 프로젝트의 작업 공간

```
<프로젝트 루트>/
├── CLAUDE.md                  # ticket-pilot 관리 블록 포함 (§4.4)
└── .ticket-pilot/
    ├── config.json            # 키 2개만 (§4.5)
    ├── tickets.json           # ★ 티켓 데이터의 유일한 원본
    ├── board.html             # 생성된 뷰 (보드 탭 + 리포트 탭)
    ├── profile.md             # 업무 프로필 — 관찰→패턴→규칙 (§4.6)
    ├── memory/
    │   ├── RECENT_MEMORY.md   # 최근 7일, 일 단위 엔트리
    │   ├── MIDDLE_MEMORY.md   # 7~30일, 주 단위 요약
    │   └── LONG_MEMORY.md     # 30일 초과, 월 단위 핵심
    └── artifacts/T-XXX/       # 증빙(스크린샷 등), 커밋 대상
```

### 3.3 데이터 흐름

```
[사용자] ─ /tp:init ─▶ project-setup
    프로젝트 스캔 + 기존 스킬 인벤토리 + 목적 인터뷰
    → CLAUDE.md 관리 블록 + .ticket-pilot/ 워크스페이스 생성

[사용자] ─ /tp:tickets ─▶ ticket-create
    목적·스캔 결과·스킬 인벤토리·프로필 규칙(§4.6) → 티켓 초안(draft) + 연계 제안(suggested)
    다단계 요청은 선행 산출물 규칙(v0.4.0·ticket-create A-3-2)으로 분해:
    분석·정리 → 정의(스펙) → 구현 → 검증 순 depends_on 체인 (예: IA 정리 → 화면별 스펙 → 화면 구현)
    → tickets.json 갱신 → board.html 재생성 (적용한 프로필 규칙은 ID로 인용)

[사용자] ─ board.html에서 승인/반려/수정/코멘트 ─▶ 저장
    (크로미움 기본) [tickets.json 연결](최초 1회) 후 [저장] → tickets.json 직접 덮어쓰기
    → 보드 재방문 시 연결 파일을 우선 로드해 저장 상태 유지, 클로드는 tickets.json을 바로 읽고 작업
    (폴백) [프롬프트 복사(코워크)] → 변경 JSON 클립보드 복사 → 채팅에 붙여넣기 → 클로드가 검증 후 반영
    → 반영·직접 저장 diff에서 관찰 추출 → profile.md 관찰 로그 기록 (§4.6)

[사용자] ─ "티켓 확인해줘" / :run ─▶ ticket-run
    approved만, 의존성·우선순위 순으로 실행 — 기본 직렬,
    독립업무 배치(상호 의존 없음 + scope 서로소, 상한 3)는 tp-worker 병렬 (§4.8)
    배치 전체 in_progress 일괄 선점(중복 방지) → 실행/스폰 → 보고 검증 → done + result 기록
    (기록 주체는 오케스트레이터 단독 — 워커는 tickets.json을 쓰지 않는다)
    수렴 티켓은 의존 전부 done 시 다음 회차에 자동 자격 획득
    소진 후 마무리(구 ticket-optimize 흡수): 후속 제안 표·blocked 요약 — 티켓 기록은 승인 후 ticket-create

[사용자] ─ /tp:done ─▶ day-close
    오늘 엔트리를 RECENT_MEMORY에 기록 → 메모리 3계층 압축(§4.3, 구 memory-optimize 흡수)
    → 프로필 관찰 임계 도달 시 규칙 승격·보고 (§4.6)
    → 소통 규칙(C-xx) 변경 시 CLAUDE.md '소통 방식' 절 동기화 (§4.4)
    → 반복 패턴 감지 시 스킬 제안(초안만)
```

### 3.4 티켓 상태 머신 [확정]

```
draft ──승인──▶ approved ──run──▶ in_progress ──완료──▶ done
  │                │                    │
  └─반려─▶ rejected └─수정─▶ draft      └─실패/보류─▶ blocked ──재승인─▶ approved

done ──재오픈(사용자 명시 요청)──▶ approved
```

규칙 3가지 [확정]:
1. 상태 변경은 반드시 tickets.json 저장을 동반한다. 대화 메모리에만 두지 않는다.
2. `in_progress`는 실행 **시작 전**에 기록한다. 세션이 끊겨도 중복 실행이 발생하지 않는다.
3. `done` 티켓은 재오픈 절차 없이는 절대 다시 실행하지 않는다.

[v0.5.0 추가] 병렬 배치에서는 배치 전 티켓을 **한 번의 저장으로 일괄 선점**하므로 `in_progress`가 동시에 여러 개 존재할 수 있다. 이때도 tickets.json을 쓰는 주체는 오케스트레이터(run 세션) 하나뿐이다 — 워커는 상태를 기록하지 않는다 (§4.8).

### 3.5 커맨드 · 스킬 · 자연어 트리거 매핑

| 커맨드 | 실행 스킬 | 자연어 트리거 예 |
|--------|-----------|------------------|
| /tp:init | project-setup | "프로젝트 세팅해줘" |
| /tp:tickets | ticket-create | "티켓 만들어줘", "보드 변경 반영해줘" |
| /tp:run | ticket-run | "티켓 확인해줘", "티켓 실행해줘", "후속 제안 정리해줘" |
| /tp:done | day-close | "오늘 마감해줘", (압축만) "메모리 정리해줘" |

---

## 4. 데이터 계약

### 4.1 tickets.json 스키마 [확정]

```json
{
  "schema_version": 3,
  "project": { "name": "", "goal": "" },
  "updated_at": "2026-07-22T18:00:00+09:00",
  "tickets": [
    {
      "id": "T-001",
      "title": "로그인 폼 유효성 검사 추가",
      "purpose": "왜 이 작업이 필요한지 1~2문장",
      "steps": [
        "구체 실행 단계 1",
        "구체 실행 단계 2",
        "검증: 완료 판정 방법 (필수, 항상 마지막)"
      ],
      "status": "draft",
      "priority": 1,
      "depends_on": [],
      "origin": "user",
      "comments": [
        { "at": "2026-07-24T09:30:00+09:00", "text": "이거 할 때 docs/style-guide.md 참고해서 진행해줘" }
      ],
      "scope": ["src/login.js"],
      "latitude": "strict",
      "created_at": "…",
      "updated_at": "…",
      "result": null
    }
  ]
}
```

- `status`: `draft | approved | in_progress | blocked | done | rejected`
- `origin`: `user`(사용자 요청 기반) | `suggested`(플러그인 제안)
- `steps` 마지막 항목은 반드시 `"검증:"`으로 시작하는 완료 판정 방법 [확정] — Backlog.md의 티켓별 수용 기준 방식 차용
- [v0.3.0 변경 · 7/23 사용자 승인] `purpose`는 필드·스키마 변경 없이 문자열 내부 컨벤션으로 "사유:/목적:/효과:" 3줄 형식을 따른다 (suggested는 첫 줄에 출처 태그). 코드·버전·정책 ID는 purpose에 넣지 않고 steps에 담는다. 구형 티켓은 재작성하지 않으며 board.html이 형식 미준수 시 원문 그대로 표시한다 (D-11 재합의 불요)
- [v0.4.0 변경 · 7/24 사용자 요청 CHANGE_REQUEST #2] `comments` 필드 추가, schema_version 1→2. **사용자 전용** 요청·코멘트 배열(각 항목 `{at, text}`) — 보드 모달·채팅으로 추가되고, ticket-run이 실행 시 지시로 반영한다(참고 문서 선독·처리 방식 준수·summary에 반영 명시, 모순 시 blocked). 에이전트는 사용자 명시 지시 없이 comments를 쓰지 않는다. 신규 티켓은 빈 배열. v1 파일은 로드 시 comments:[] 보충으로 무손실 승격. D-11(그 외 필드 추가 금지)은 유지
- [v0.5.0 변경 · 7/24 사용자 요청 CHANGE_REQUEST #5] `scope`·`latitude` 필드 추가, schema_version 2→3. `scope` = 이 티켓이 수정할 파일·디렉터리 배열(읽기 전용 대상 제외) — 병렬 독립 판정(§4.8)의 근거이며 **빈 배열이면 병렬 제외(직렬 전용)**. `latitude` = 실행 재량 `strict`(기본: steps 외 금지) | `flex`(scope 안 부수 개선 허용·내역 구분 보고) — 설정은 사용자 명시 지시·보드 편집으로만, **프로필 기반 자동 설정은 보류** (사용자 결정 7/24: 기계적 학습 연동은 피로도 우려로 추후 검토). 1·2 파일은 로드 시 누락 필드 보충(comments:[]·scope:[]·latitude:"strict")으로 무손실 승격. D-11 유지
- `result`는 done 시에만 채운다:

```json
{
  "summary": "무엇을 했는지 3문장 이내",
  "files_changed": ["src/login.js"],
  "evidence": [
    { "type": "screenshot", "path": "artifacts/T-001/shot1.png", "note": "" }
  ],
  "followups": ["연계 제안 요약 (ticket-run 마무리가 표로 제안, 티켓화는 승인 후 ticket-create)"],
  "completed_at": "…"
}
```

- `evidence.type`: `screenshot | diff | file`
- **필드 추가 금지** [확정, D-11]

### 4.2 board.html ↔ tickets.json 계약

- **재생성 시점**: ticket-create, ticket-run(각 티켓 완료 직후), day-close가 항상 board.html을 재생성한다. 사람이 board.html을 직접 고쳐도 다음 재생성 때 사라진다는 사실을 HTML 상단에 명시한다.
- **데이터 임베드**: `<script id="tickets-data" type="application/json">…</script>`에 tickets.json 전문을 넣는다.
- **보드 탭**: 상태별 4컬럼(초안 / 승인 / 진행중 / 완료). 카드에 title·purpose·steps·priority 표시. 카드 액션: 승인, 반려, 편집(제목·목적·steps·priority), draft로 되돌리기.
  - [v0.2.0 변경 · 7/23 사용자 승인] 카드 액션·편집은 카드 클릭 시 열리는 상세 모달에서 수행(인라인 폼 제거). 드래그로 초안 ↔ 승인 컬럼 간 상태 전환 지원 — 진행중·완료 컬럼은 드롭 불가, 반려 전환은 버튼만. 허용 전환 규칙은 기존과 동일. 컬럼별 독립 스크롤·상태별 파스텔 카드 배경 추가. (비목표의 "드래그앤드롭 정렬"은 컬럼 내 순서 변경을 뜻하며 여전히 미지원)
  - [v0.4.0 변경 · 7/24 사용자 요청 CHANGE_REQUEST #2] 모달은 2패널 — 좌: 티켓 내용, **우: "요청·코멘트" 패널**(380px·독립 스크롤·입력칸 min 260px 세로 리사이즈 — 긴 요청 전제. 920px 이하에서는 내용 아래 1열). 전 상태에서 표시, 추가·삭제는 편집 가능 상태(draft·approved·rejected)에서만. 카드에 💬 n 배지. 액션 버튼(승인·반려·편집/저장·취소)은 모달 하단 전체 폭 푸터에 **우측 정렬 — 주 액션(승인/저장)이 제일 우측**.
  - [v0.5.0 변경 · 7/24 CHANGE_REQUEST #5] 모달 본문에 "수정 범위(scope)" 칩과 "재량" 표기(미지정 시 "병렬 제외" 안내), 편집 폼에 scope(한 줄에 하나)·재량 select 추가. latitude=flex 카드에 "재량" 배지. 허용 편집 필드에 scope·latitude 포함 (§4.1 검증 규칙 준수).
- **리포트 탭**: done 티켓 표. 열 = ID · 제목 · 목적 · 수행 요약 · 변경 파일 · 증빙(썸네일, 클릭 시 원본) · 후속 제안.
- **저장 UX** [확정, D-02 → v0.4.0에서 직접 저장 경로로 확장 · 7/24 사용자 요청 CHANGE_REQUEST #3]:
  1. 크로미움 (권장): [tickets.json 연결] 버튼(`showOpenFilePicker`, 최초 1회) → 핸들을 IndexedDB에 보존 → 이후 보드 로드 시 연결 파일을 우선 읽어 렌더(권한 'granted'면 무클릭, 'prompt'면 1클릭 배너 — 크롬 122+ "모든 방문에서 허용" 시 이후 무클릭). [저장] 버튼이 tickets.json을 직접 덮어쓴다(변경 티켓·파일 updated_at 스탬프, 저장 후 변경 기준선 리셋). 저장 전 파일 updated_at을 재확인해 다른 세션 변경 감지 시 [파일 다시 불러오기]/[그래도 저장] 선택 UI. 미저장 변경이 있으면 beforeunload 경고.
  2. 폴백 (사파리·파이어폭스 등 전 브라우저): "프롬프트 복사(코워크)" 버튼 → 변경된 tickets 전체 JSON을 클립보드에 복사하고, "클로드 채팅에 붙여넣고 '보드 변경 반영해줘'라고 입력하세요" 안내를 표시
  3. 채팅 반영 시 클로드는 §4.2 무결성 규칙으로 검증하고, 위반 항목은 사유와 함께 거부한다. 직접 저장분은 보드가 동일 규칙을 클라이언트에서 강제하므로 재검증 없이 원본으로 인정하되, 에이전트는 board.html 임베드 스냅샷과의 diff에서 관찰(profile.md)을 추출하고 실행 상태 티켓의 변경이 발견되면(손편집 신호) 경고한다
- **무결성 규칙** [확정]: HTML에서 허용되는 변경은 `draft ↔ approved ↔ rejected` 간 전환과 필드 편집(comments 포함)뿐이다. `in_progress`·`done` 카드는 읽기 전용이다. 실행 상태는 에이전트만 쓴다.
- **구현 제약** [유력안]: 외부 CDN 의존 0, 바닐라 JS 단일 파일. 오프라인에서 완전 동작해야 한다. (File System Access API·IndexedDB는 로컬 기능이라 이 제약과 충돌하지 않는다)

### 4.3 메모리 파일 형식

RECENT_MEMORY.md의 일 엔트리 템플릿 [확정]:

```markdown
## 2026-07-22 (수)
### 완료
- T-003 로그인 유효성 검사 — 결과 요약 1줄
### 결정
- (없으면 섹션 생략)
### 미해결 / 이슈
- …
### 내일 후보
- …
```

압축 규칙 [확정, D-07] — /done 실행 시:
1. 오늘 엔트리를 RECENT 상단에 추가 (당일 엔트리가 이미 있으면 병합, 중복 생성 금지)
2. RECENT에서 7일 초과 엔트리들을 **주 단위 1개 섹션**으로 요약해 MIDDLE 상단에 추가하고 RECENT에서 제거. 요약 분량은 원문의 1/4 이하
3. MIDDLE에서 30일 초과 섹션을 **월 단위**로 LONG에 병합하고 MIDDLE에서 제거. 지금도 유효한 결정과 프로젝트 궤적만 남긴다 (1~2문단)
4. LONG은 자동 삭제하지 않는다. 300줄 초과 시 사용자에게 정리를 제안만 한다
5. 압축은 자동 수행 후 결과를 보고한다 ("이동 n건, RECENT x줄 → y줄"). 원문 복구는 git 히스토리로 (D-09)

### 4.4 CLAUDE.md 관리 블록 [유력안 — 문구는 Step 1.3에서 확정]

```markdown
<!-- ticket-pilot:begin — 이 블록은 ticket-pilot이 관리합니다. init 재실행 시 갱신됩니다 -->
# 프로젝트 컨텍스트 (ticket-pilot)

## 배경과 목적
- 목적: {init에서 작성}
- 사용자/이해관계자: {…}
- 성공 기준: SC-1 {…} / SC-2 {…} — 반드시 번호 목록으로 쓴다 (§4.7 S4 출처가 티켓과 대응시키는 기준)

## 작업 규칙
1. 실행 작업은 .ticket-pilot/tickets.json의 approved 티켓에 근거한다.
   티켓 없는 실행 요청을 받으면 먼저 티켓 초안을 제안한다.
2. 지시·계획 문서는 하위 모델이 추가 맥락 없이 수행할 수 있게 쓴다.
   순서: 목적 → 입력 → 실행 단계 → 완료 기준(검증 방법).
3. 세션 시작 시 .ticket-pilot/memory/RECENT_MEMORY.md의 최근 3일 엔트리를 확인한다.
4. CLAUDE.md 전체는 200줄을 넘기지 않는다. 넘칠 내용은 별도 문서로 분리해 링크한다.

## 소통 방식 (기본값 + 학습 규칙 · day-close가 동기화)
적용 범위: 사용자에게 보이는 글 — 보고, 제안, 논의, 티켓의 title·purpose. 하위 모델용 지시문(steps)과 코드는 정밀성을 우선한다.
1. 약어·전문 용어는 처음 쓸 때 한 줄로 풀어 쓴다. 파일·함수명은 역할과 함께 말한다.
2. 보고와 제안은 결론 → 근거 → 세부 순서로 쓴다.
3. 용어 설명·재설명·"쉽게/짧게" 요청을 받으면 .ticket-pilot/profile.md 관찰 로그에 1줄 기록한다.
{승격된 소통 규칙 C-xx가 이 아래에 추가된다}

## 명령
/tp:init · :tickets · :run · :done
<!-- ticket-pilot:end -->
```

쓰기 규칙 [확정]: begin/end 마커 사이만 교체한다. 마커가 없으면 파일 끝에 블록을 추가한다. 블록 자체는 60줄 이하를 유지한다. 마커 밖의 사용자 작성 내용은 절대 수정하지 않는다. day-close는 마커 블록 안에서도 '소통 방식' 절만 갱신할 수 있다 (그 외 절은 init 전용). 소통 방식 절은 12줄 이하를 유지한다 [유력안].

### 4.5 config.json [확정]

```json
{ "screenshot": "auto", "memory": { "recent_days": 7, "middle_days": 30 } }
```

- `screenshot`: `auto | off`
- 키 추가 금지

### 4.6 profile.md — 업무 프로필 [v0.3 추가]

위치: `.ticket-pilot/profile.md` · 소유: 플러그인이 관리하되, 사용자가 직접 수정하면 그대로 존중한다.

목적: 사용자의 작업 방식과 결정 경향을 **근거 있는 규칙**으로 객관화해, 티켓 초안과 연계 제안(suggested)을 개인화한다.

구조 — 관찰 → 패턴 → 규칙 3계층 [확정, D-15]:

```markdown
# 업무 프로필 (ticket-pilot 관리 · 마지막 갱신: 2026-07-29)

## 규칙 — 작업 (티켓 생성 시 적용)
- R-01 배포 계열 suggested 제안 제외 — 근거: 반려 3건 (7/23, 7/25×2)
- R-02 티켓 steps는 5개 이하로 분할 — 근거: steps 축소 편집 3건 (7/24~7/28)

## 규칙 — 소통 (day-close가 CLAUDE.md에 동기화)
- C-01 새 개념 설명에는 실무 비유 1개 곁들이기 — 근거: 용어 재설명 요청 3건 (7/24, 7/26×2)

## 패턴 (승격 대기 후보)
- P-01 문서화 티켓 승인율 높음 (승인 2/2) — 관찰 1건 더 쌓이면 규칙 후보

## 관찰 로그 (원시 신호 · 승격되면 근거 요약만 남기고 삭제)
- 2026-07-29 T-012(suggested·배포) 반려
- 2026-07-29 T-010 steps 7→4개로 축소 편집
- 2026-07-26 "suggested가 뭐야?" 용어 질문
```

운영 규칙:
1. 모든 규칙·패턴은 관찰 근거(날짜·티켓 ID)를 인용한다. 근거 없는 항목은 기록 금지 [확정, D-15]
2. 승격 임계: 동일 유형 관찰 3회 이상 · 2일 이상에 걸쳐 등장 (Step 4.3 스킬 제안과 동일) [유력안] — 하루치 배치 반려만으로 규칙이 생기는 과적합을 막는다. 명시 지시는 예외 (규칙 8)
3. 파일 100줄 상한. 초과 시 승격이 끝난 관찰부터 정리한다 [유력안]
4. 시드(콜드 스타트) [유력안]: 프로필이 비어 있을 때 최초 ticket-create 실행 시 — (1) 프로젝트에 의사결정·스타일 지식파일이 있으면 참조를 먼저 제안 (2) 없으면 3문항 이내 시드 질문 (후보: 티켓 크기 선호, 제안 적극성, 설명 수준 — 용어 풀이 필요 여부). 시드 응답은 임계 없이 즉시 규칙으로 기록한다 (근거: "시드 인터뷰 · 날짜")
5. 작업 규칙(R-xx)의 소비처는 ticket-create뿐이며, 적용한 규칙은 제안 문구에 ID로 인용한다 ("R-01에 따라 배포 계열 제안 제외"). 소통 규칙(C-xx)은 day-close가 CLAUDE.md '소통 방식' 절에 동기화해 전 세션에 적용한다 (D-23). **ticket-run은 프로필을 직접 참조하지 않는다** [확정, D-16]
6. 승격·변경된 규칙은 day-close가 근거와 함께 보고한다. 사용자가 거부하면 규칙을 취소선(`~~R-01~~ 거부 7/30`)으로 보존한다 — 거부된 규칙은 자동 재승격되지 않으며, 재승격은 사용자 확인을 거쳐서만 한다. 완전 삭제는 명시 요청 시에만 [유력안, D-17]
7. 소통 관찰 신호는 §4.4 소통 방식 3번 규칙으로 모든 세션에서 수집된다 — 용어 질문, 재설명 요청, "쉽게/짧게/자세히" 피드백. C-xx는 표현·설명 방식만 규정할 수 있으며, 작업이나 실행을 지시하는 문구는 CLAUDE.md 동기화를 거부한다 [확정, D-23]
8. 명시 지시의 즉시 규칙화 [확정, D-25]: "앞으로 · 항상 · 매번 · 하지 마" 같은 일반화 표지가 있는 사용자 지시는 임계 없이 즉시 규칙으로 기록한다 (근거: 발화 인용 · 날짜). 일반화 표지가 없는 일회성 지시("이번엔 빼줘")는 관찰 1건으로만 기록한다
9. 반례 학습 [유력안, D-26]: 활성 규칙과 모순되는 사용자 행동(예: R-01이 제외하는 계열의 티켓을 직접 생성·요청)도 관찰 로그에 기록한다. 반례가 3회 이상 · 2일 이상 누적되면 day-close가 해당 규칙의 재검토를 제안한다. 자동 해제는 금지 — 해제는 사용자 결정으로만

### 4.7 연계 제안 방법론 — 제안 파이프라인 [v0.4 추가]

원칙 [확정, D-19]: 모든 suggested 티켓은 **프로젝트 내부의 구체 신호**에서만 나온다. 출처를 인용하지 못하거나 "왜 지금인가"에 답하지 못하는 후보는 제안하지 않는다. 일반론(테스트 추가, 리팩토링, 문서화 등)은 그 자체로는 출처가 아니며, 아래 출처를 통해 구체 증거가 있을 때만 진입한다.

파이프라인: **후보 수집 → 필터 → 랭킹 → 근거 명시** 4단계.

**1) 후보 출처 — 근거 강도 티어순 [확정]**

| 티어 | 출처 | 정의 | 신호의 성격 |
|------|------|------|-------------|
| T1 | S1 메모리 미해결 | RECENT·MIDDLE의 "미해결/이슈"·"내일 후보" 중 티켓화되지 않은 항목 | 사용자가 직접 적은 것 |
| T1 | S2 블록 해제 | blocked 티켓의 재개 조건을 만드는 선행 작업 | 사용자가 승인했던 일의 장애물 |
| T2 | S3 후속 연쇄 | done 티켓의 result.followups (ticket-run 마무리 수집분) | 실제 작업에서 파생 |
| T3 | S4 성공 기준 갭 | CLAUDE.md 성공 기준 중 대응 티켓(draft/approved/done)이 없는 항목 | 목적에서 역산 |
| T4 | S5 스캔 갭 | 파일 스캔에서 발견된 구체 결함 — 목적과의 연결을 명시할 수 있을 때만 | 프로젝트 상태 관찰 |

기존 스킬 인벤토리는 출처가 아니라 **가점 요소**다: 동티어 후보 중 기존 스킬로 즉시 처리 가능한 것을 우선하고, steps에 해당 스킬명을 명시한다.

**2) 필터 — 하나라도 걸리면 탈락 [확정]**
- F1 프로필 규칙: 활성 R-xx가 제외하는 계열 (§4.6)
- F2 반려 이력: rejected 티켓과 실질적으로 같은 제안 (부활은 사용자 명시 요청 시에만)
- F3 중복: 기존 draft·approved·in_progress 티켓과 중복
- F4 크기: 1세션 초과 — 첫 단계만 잘라 제안하거나 제외
- F5 근거 결격: 출처 인용 또는 "왜 지금" 1줄을 채울 수 없는 후보. **이 필터가 일반론 제안을 막는 핵심 장치다**

**3) 랭킹 [유력안, D-20]**
- 1순위: 티어 (T1 > T2 > T3 > T4)
- 동티어 우선순위: 기존 스킬 가점 → 프로필 패턴(승인율 높은 계열, §4.6) → 최신 신호
- 구성 제약: 상한 3건 중 동일 출처 최대 2건 (제안 다양성 확보) [유력안]
- 신뢰 예산 [유력안, D-21]: suggested 최근 10건의 승인율(승인/(승인+반려))이 1/3 미만이면 상한을 3→1로 낮춘다. day-close가 이 상태를 보고하고, 회복 시 3건으로 복원한다

**4) 근거 명시 형식 [확정]**
- 대화 제안 시 각 후보에 3요소를 붙인다: ① 출처 태그와 인용(날짜·티켓 ID·파일) ② "왜 지금" 1줄 ③ 적용된 프로필 규칙 ID
- purpose 기록 형식: `[S3·T-003 후속] 로그인 실패 시 에러 안내가 없음` 처럼 출처 태그를 접두로 넣는다. 필드 추가 없이 D-11을 유지하는 방법이다
  - [v0.3.0 변경 · 7/23 사용자 승인] 출처 태그는 purpose 첫 줄에 단독으로 쓰고, 그다음 줄부터 사유/목적/효과 3줄 형식을 따른다 (§4.1의 v0.3.0 변경 참조). S7 판정(출처 태그 존재)은 첫 줄 기준으로 동일하게 유효
- 검증 가능성: S7 판정은 tickets.json의 suggested 티켓 purpose에 출처 태그가 전부 있는지로 확인한다

### 4.8 독립업무 병렬 실행 계약 [v0.5.0 추가 · 7/24 CHANGE_REQUEST #5]

원칙: **병렬은 선언(scope)으로 증명된 독립에만, 기록은 오케스트레이터 하나만.**

1. **독립 판정** (ticket-run 배치 선정): approved & 의존 전부 done인 티켓을 priority 순으로 스캔, ① 상호 depends_on 없음(직·간접) ② scope 서로소(동일 경로·상위/하위 디렉터리 관계 = 겹침) ③ scope 비어 있지 않음 — 을 모두 만족하는 티켓을 배치에 추가. **상한 3건.** scope 미선언 티켓은 항상 단독 직렬.
2. **선점**: 배치 전체를 in_progress로 **1회 저장**으로 일괄 선점 (§3.4). 동시 세션 가드(updated_at 비교)는 기존과 동일.
3. **워커 위임**: 티켓당 tp-worker 1개를 동시 스폰. 에이전트 타입 `tp-worker`(agents/tp-worker.md)가 없으면 범용 에이전트 + 규칙 전문 포함, 서브에이전트 도구 자체가 없으면 **직렬 폴백**. 지시서는 자족적(목적·steps·comments·scope·재량 조항·보고 JSON 형식·SC-x 발췌).
4. **워커 금지 규칙** (tp-worker.md에 내장, 지시서보다 우선): scope+자기 artifacts 외 수정 금지 · .ticket-pilot 데이터 파일 수정 금지 · git 커밋 금지 · 타 티켓 작업 금지 · 마지막 메시지는 보고 JSON만.
5. **보고 검증** (오케스트레이터, done 기록 전): ① files_changed ⊆ scope(+artifacts) ② strict면 steps 무관 변경 없음 ③ ok=true — 하나라도 실패 시 done이 아니라 **blocked + 사유** 기록 (자동 되돌리기 없음, 커밋 전이므로 git 복구 안내). flex의 discretionary는 summary에 "재량:" 접두로 병기.
6. **수렴(fan-in)**: 묶음의 후속 작업은 ticket-create가 생성 시점에 수렴 티켓으로 함께 만들고 depends_on=[묶음]으로 연결 (A-3-3). 실행 자격 판정은 기존 규칙 그대로.
7. **재량(latitude) 거버넌스**: 기본 strict. flex 전환은 사용자 명시 지시·보드 편집만. **프로필(R-xx) 기반 자동 설정·학습 승격은 보류** [Open Issue — 사용자 결정 7/24: 과도한 자동화로 사용 피로 우려].

---

