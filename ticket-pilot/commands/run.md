---
description: 승인된 티켓 순차 실행 — 중복 방지 프로토콜에 따라 approved 티켓을 1건씩 실행하고 리포트 갱신
---
Skill 도구로 `tp:ticket-run` 스킬을 호출해 그 절차를 순서대로 그대로 실행하라. Skill 도구를 쓸 수 없으면 ${CLAUDE_PLUGIN_ROOT}/skills/ticket-run/SKILL.md 를 읽어 실행하라.
사용자 추가 입력: $ARGUMENTS
